# 📌 Application Name
spring.application.name=bookshare

# 📌 Server Port (Default is 8080)
server.port=8080

# 📌 MySQL Database Configuration
spring.datasource.url=jdbc:mysql://sql.freedb.tech:3306/freedb_book_share
spring.datasource.username=freedb_user_book
spring.datasource.password=7Wap38QW6Rzh#Cv
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 📌 Hikari Connection Pool Settings (Recommended)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=25000
spring.datasource.hikari.connection-timeout=15000
spring.datasource.hikari.max-lifetime=1800000

# 📌 Hibernate JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=create
# Change to 'update' so it doesn’t drop tables on restart
spring.jpa.properties.hibernate.format_sql=true

# 📌 Logging (For debugging)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
